*_topo.txt files gives the network topology. Each line represents an edge in the form "{node1}\t{node2}\t{bandwidth of the link}".
*_nodeCapa.txt files the node capacities of the network. Each line represents a node and its capacity, in the form "{node}\t{capacity of the link}".
*_demand.txt files gives the demands to satisfy. Each link represents a demand, in the form "{source}\t{destination}\t{load}\t{service chain}".
